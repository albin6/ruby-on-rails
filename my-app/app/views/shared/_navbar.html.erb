<nav class="bg-white shadow-md">
  <div class="container mx-auto px-4 py-3 flex items-center justify-between">
    
    <!-- Left: Brand -->
    <div class="flex-shrink-0">
      <%= link_to "MyBrand", root_path, class: "text-2xl font-bold text-blue-600 hover:text-blue-800" %>
    </div>
    
    <!-- Center: Nav links -->
    <div class="hidden md:flex space-x-6">
      <%= link_to "Home", root_path, class: "text-gray-700 hover:text-blue-600" %>
      <%= link_to "About", about_path, class: "text-gray-700 hover:text-blue-600" %>
    </div>
    
    <!-- Right: Auth / User -->
    <div class="flex items-center space-x-4">
      <% if current_user %>
        <!-- User Icon -->
        <%= link_to profile_path, class: "text-gray-700 hover:text-blue-600" do %>
          <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5.121 17.804A13.937 13.937 0 0112 15c2.5 0 4.847.657 6.879 1.804M15 10a3 3 0 11-6 0 3 3 0 016 0z" />
          </svg>
        <% end %>
      <% else %>
        <%= link_to "Login", login_path, class: "px-3 py-1 text-sm font-medium text-gray-700 hover:text-blue-600" %>
        <%= link_to "Sign Up", signup_path, class: "px-3 py-1 text-sm font-medium bg-blue-600 text-white rounded hover:bg-blue-700" %>
      <% end %>
    </div>
    
  </div>
</nav>